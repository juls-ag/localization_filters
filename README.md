# localization_filters
Robot state estimation using EKF, UKF and Monte Carlo Localization in MATLAB.

The simulation generates a ground truth trajectory and simulates noisy sensors (GPS and IMU). 
Each filter estimates the robot pose, and the results are compared using the following metrics:

- RMSE
- Maximum Error
- Standard Deviation
